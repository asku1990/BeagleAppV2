import type { PDFPage } from "pdf-lib";

export function drawTrialDogPdfPhase3(input: { page: PDFPage }): void {
  void input;
  // Phase 3 will cover the parent and owner block once those fields are wired in.
}
